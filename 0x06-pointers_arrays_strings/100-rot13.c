#include "main.h"
#include <stdio.h>
#include <string.h>
#include <ctype.h>

/*
 * rot13 - check the code.
 *
 * @str: check the paarmeter.
 *
 * Return: check the code.
 */

char *rot13(char *str)
{
	unsigned long int i;
	char arr[55] = 

	for (i = 0 ; i < strlen(str) ; i++)
	{
		if (isalpha(str[i]))
			if (str[i] > 'M')	
	}
	return (str);
}
