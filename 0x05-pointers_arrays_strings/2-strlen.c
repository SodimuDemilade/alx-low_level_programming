#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - check the code.
 *
 * @s: check the parameter.
 *
 * Return: check the code.
 *
 */

int _strlen(char *s)
{
	return (strlen(*s));

}
