#include <stdio.h>

/**
 * main - check the code"
 *
 * @argc: check the parameters.
 *
 * @argv: check the parameters.
 *
 * Return: check the code.
 *
 */

int main(int argc, char *argv[])
{
	void(argc);
	printf("%s\n", argv[0]);
	return (0);
}
