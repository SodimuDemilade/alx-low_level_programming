#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: 'the program's description'
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	for (int i = 'a' ; i <= 'z' ; i++)
	{
		putchar(i);
	}
	for (int i = 'A' ; i <= 'Z' ; i++)
	{
		putchar(i);
	}
	return (0);
}
